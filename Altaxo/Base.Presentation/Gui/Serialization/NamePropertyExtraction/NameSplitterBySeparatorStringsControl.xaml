<UserControl x:Class="Altaxo.Gui.Serialization.NamePropertyExtraction.NameSplitterBySeparatorStringsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:localm="clr-namespace:Altaxo.Gui.Serialization.NamePropertyExtraction;assembly=AltaxoBase"
             d:DataContext="{d:DesignInstance Type=localm:NameSplitterBySeparatorStringsController}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="4"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="4"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Separator strings:"/>
        <DataGrid Grid.Row="2" Grid.Column="0" ItemsSource="{Binding Separators}" AutoGenerateColumns="False" CanUserAddRows="True" CanUserDeleteRows="True" MinHeight="80">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Separator" Binding="{Binding Separator, Mode=TwoWay}" Width="100" IsReadOnly="False"/>
                <DataGridTextColumn Header="Display name" Binding="{Binding DisplayName}" Width="100" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <CheckBox Grid.Row="4" Grid.Column="0" Content="Remove empty entries" IsChecked="{Binding RemoveEmptyEntries}"/>
    </Grid>
</UserControl>
