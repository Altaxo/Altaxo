<UserControl x:Class="Altaxo.Gui.Graph.ExchangeTablesOfPlotItemsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<UserControl.Resources>

		<ContextMenu x:Key="EmptyContextMenu">
			<MenuItem Header="Choose new table..." Click="EhChooseTable"/>
			<MenuItem Header="Choose new folder..." Click="EhChooseFolder"/>
		</ContextMenu>

		<Style x:Key="MyContainer" TargetType="{x:Type ListViewItem}">
			<Setter Property="ContextMenu" Value="{StaticResource EmptyContextMenu}"/>
			<Setter Property="Margin" Value="0,1,0,0"/>
			<Setter Property="Height" Value="21"/>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="true">
					<Setter Property="Foreground" Value="Blue" />
					<Setter Property="Cursor" Value="Hand"/>
				</Trigger>
				<MultiTrigger>
					<MultiTrigger.Conditions>
						<Condition Property="IsSelected" Value="true" />
						<Condition Property="Selector.IsSelectionActive" Value="true" />
					</MultiTrigger.Conditions>
					<Setter Property="Foreground" Value="Yellow" />
				</MultiTrigger>
			</Style.Triggers>
		</Style>
	</UserControl.Resources>
    <Grid>
		<ListView x:Name="_guiTableList" ItemContainerStyle="{StaticResource MyContainer}" PreviewMouseRightButtonDown="EhTableList_MouseRightButtonDown" >
			<ListView.View>
				<GridView>
						<GridViewColumn Header="Current table" DisplayMemberBinding="{Binding Text}"/>
						<GridViewColumn Header="New table" DisplayMemberBinding="{Binding Text1}"/>
				</GridView>
			</ListView.View>
		</ListView>
	</Grid>
</UserControl>
