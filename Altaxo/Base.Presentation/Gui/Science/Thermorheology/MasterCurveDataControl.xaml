<UserControl x:Class="Altaxo.Gui.Science.Thermorheology.MasterCurveDataControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Altaxo.Gui.Science.Thermorheology"
             xmlns:localm="clr-namespace:Altaxo.Gui.Science.Thermorheology;assembly=AltaxoBase"
       			 xmlns:axogc="clr-namespace:Altaxo.Gui.Common"
       			 xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop"
       			 xmlns:axocoll="clr-namespace:Altaxo.Collections;assembly=AltaxoBase"
             d:DataContext="{d:DesignInstance Type=localm:MasterCurveDataController}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
    <Grid.Resources>
      <ResourceDictionary Source="pack://application:,,,/AltaxoBase.Presentation;component/CommonStyles.xaml" />
    </Grid.Resources>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="4" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="4" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="4" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="4" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="4" />
      <RowDefinition Height="Auto" />
      <RowDefinition MinHeight="4" Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="4" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="4" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="4" />
    </Grid.RowDefinitions>


    <Label Grid.Row="0" Grid.Column="0" Content="Available data:"/>
    <axogc:MultiSelectTreeView	
            Grid.Row="2" Grid.Column="0" Grid.RowSpan="18"
            ItemsSource="{Binding AvailableContent.Nodes}"
            Focusable="True"
			dd:DragDrop.IsDragSource="True" 
            dd:DragDrop.DragMVVMHandler="{Binding AvailableItemsDragHandler}"
            >
      <ItemsControl.ItemContainerStyle>
        <!-- This Style binds a TreeViewItem to a TreeViewItemViewModel. -->
        <Style TargetType="{x:Type TreeViewItem}">
          <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
          <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
          <Setter Property="FontWeight" Value="Normal" />
          <Style.Triggers>
            <DataTrigger Binding="{Binding IsSelected}" Value="True">
              <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </ItemsControl.ItemContainerStyle>

      <ItemsControl.Resources>
        <HierarchicalDataTemplate DataType="{x:Type axocoll:NGTreeNode}" ItemsSource="{Binding Nodes}">
          <StackPanel Orientation="Horizontal">
            <Image Width="16" Height="16" Margin="3,0" />
            <TextBlock Text="{Binding Text}" />
          </StackPanel>
        </HierarchicalDataTemplate>
      </ItemsControl.Resources>
    </axogc:MultiSelectTreeView>

    <Button Grid.Row="2" Grid.Column="1"  
                Height="{Binding ElementName=_btGroup, Path=ActualHeight}" 
                Margin="{StaticResource ButtonMargin}"
                Command="{Binding CmdPutDataToPlotItems}"
                Click="EhFocusData" 
                HorizontalContentAlignment="Stretch" 
                VerticalContentAlignment="Stretch"
                ToolTip="Add the selected data columns as plot item.">
      <Viewbox Style="{StaticResource ArrowViewBoxStyle}">
        <Path Style="{StaticResource LeftArrowPathStyle}" />
      </Viewbox>
    </Button>

      <Label Grid.Column="2" Grid.Row="0">Master data:</Label>
    <TabControl Grid.Column="2" Grid.Row="1" 
                ItemsSource="{Binding DataGroup.Items}"
                SelectedItem="{Binding DataGroup.SelectedItem, Mode=TwoWay}"
                DisplayMemberPath="Text">
    </TabControl>
    <ListView  Grid.Column="2" Grid.Row="2" Grid.RowSpan="18" 
               ItemsSource="{Binding DataItems.Items}"
               SelectedItem="{Binding DataItems.SelectedItem, Mode=TwoWay}">
      <ListView.Resources>
        <Style TargetType="{x:Type ListViewItem}">
          <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
        </Style>
      </ListView.Resources>

      <ListView.View>
        <GridView>
          <GridView.Columns>
            <GridViewColumn Header="No." DisplayMemberBinding="{Binding Number}"/>
            <GridViewColumn Header="Data1" DisplayMemberBinding="{Binding Data1}" Width="{Binding ColumnWidthData1, Mode=TwoWay}"/>
            <GridViewColumn Header="Data2" DisplayMemberBinding="{Binding Data2}" Width="{Binding ColumnWidthData2, Mode=TwoWay}"/>
            <GridViewColumn Header="Item" DisplayMemberBinding="{Binding Text}"/>
          </GridView.Columns>
        </GridView>
      </ListView.View>
      
    </ListView>

  </Grid>
</UserControl>
