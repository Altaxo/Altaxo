<UserControl x:Class="Altaxo.Gui.Pads.ProjectBrowser.ProjectBrowseControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition Height="Auto"/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<TreeView Grid.Row="0" Name="_treeView" TreeViewItem.Selected="EhTreeNodeAfterSelect" >
		</TreeView>
		<GridSplitter Grid.Row ="1" Background="Blue" Height="5"
                HorizontalAlignment="Stretch" VerticalAlignment="Center" ShowsPreview="true"
              ResizeDirection="Rows"/>

		<ListView Grid.Row="2" Name="_listView" SelectionChanged="EhListViewSelectionChanged" MouseDoubleClick="EhListViewItemDoubleClick">
			<ListView.Resources>
				<DataTemplate x:Key="ColumnWithIconTemplate">
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto"/>
							<ColumnDefinition/>
						</Grid.ColumnDefinitions>
						<Image Grid.Column="0"  Source="{Binding Path=ImageSource}"/>
						<TextBlock Grid.Column="1" Margin="4,0" Text="{Binding Path=Col0}"/>
					</Grid>
				</DataTemplate>
			</ListView.Resources>
			<ListView.View>
				<GridView>
					<GridViewColumn CellTemplate="{StaticResource ColumnWithIconTemplate}" x:Name="_listViewCol0" Header="Name" Width="200" />
				</GridView>
			</ListView.View>
		</ListView>
	</Grid>
</UserControl>
